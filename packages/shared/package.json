{
  "name": "@portalhop/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "comment": "Ships TypeScript source rather than a build artifact. Both consumers already transpile: Next via Turbopack, Expo via Metro/Babel. A build step here would only add a stale-output failure mode.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json",
    "./*": "./src/*.ts"
  },
  "sideEffects": false
}
