// The module is reached through requireOptionalNativeModule in lib/logo-style.ts
// rather than re-exported here, so that a build without the native side degrades
// to leaving every logo as drawn instead of throwing at import.
export {}
